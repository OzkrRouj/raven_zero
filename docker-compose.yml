services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - REDIS_URL=redis://valkey:6379/0
    volumes:
      - ./storage:/app/storage
    depends_on:
      - valkey

  valkey:
    image: valkey/valkey:9-alpine
    ports:
      - "6379:6379"
